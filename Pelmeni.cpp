#include <iostream>

using namespace std;

int main()
{
    setlocale(0, "");
    int p, t, at, qp, qt, qf, Qp, np;
    cout << "Введите \n";
    cin >> p >> t >> at >> qp >> qt >> qf;
    Qp = p / (2 * t);
    np = Qp / qp;
    ///
}